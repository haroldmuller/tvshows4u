<%= form_for(tv_show_comment) do |f| %>
  <% if tv_show_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tv_show_comment.errors.count, "error") %> prohibited this tv_show_comment from being saved:</h2>

      <ul>
      <% tv_show_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id%>
    <%= f.text_field :user_id, value: current_user.id, readonly: :true %>
  </div>

  <div class="field">
    <%= f.label :tv_show_id%>
    <%= f.text_field :tv_show_id, value: params["tv_show_id"], readonly: :true %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit :Agregar, class: "btn btn-danger", style: "color:white" %>
  </div>
<% end %>
